package com.socialred2025.users.domain;

// This code snippet is defining an enumeration type called `RoleType` in Java. It contains two
// possible values: `USER` and `ADMIN`. Enumerations are used to define a fixed set of constants. In
// this case, `RoleType` is used to represent different roles that a user can have in a system,
// specifically `USER` and `ADMIN`.
public enum RoleType {
    USER,
    ADMIN
}
